import React, { useState } from 'react';

const App = () => {
  const [word, setWord] = useState(null);
  
  return (
    <div className="App">
      <header className="App-header">
        App
      </header>
    </div>
  );
}

export default App;
