import React from "react";

type Props = {
    board: String[];
  };

const Board: React.FC<Props> = ( board ) => {
    return (
        <div>
            Board
        </div>
    )
};

export default Board;